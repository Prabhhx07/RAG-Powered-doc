name: Keep Services Warm

on:
  schedule:
    - cron: "0 6 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health + DB
        run: |
          curl -sf https://rag-powered-doc.onrender.com/health
          curl -sf https://rag-powered-doc.onrender.com/health/db
